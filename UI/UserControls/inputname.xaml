<UserControl x:Class="krushtype.UI.UserControls.inputname"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:krushtype.UI.UserControls"
             xmlns:vm ="clr-namespace:krushtype.Core.ViewModels"
             mc:Ignorable="d"
             x:Name="inputnick_uc"
             d:DesignHeight="40" d:DesignWidth="190">
    <Grid>
        <Border 
            Background="{DynamicResource SubAltColorBrush}" 
            CornerRadius="10"
            Width="190"
            Height="40"
            Padding="5"
            HorizontalAlignment="Left">
            <Grid>
                <TextBox 
                 Text="{Binding NickName, ElementName=inputnick_uc}"
                 Background="Transparent"
                 BorderThickness="0"
                 Foreground="{DynamicResource TextColorBrush}"
                 FontSize="16"
                 VerticalAlignment="Center"
                 FontFamily="pack://application:,,,/krushtype;component/Fonts/#Roboto Mono"
                 Margin="1,0,0,0"
                 x:Name="NameBox"
                 MaxLength="15"
                 Width="auto"/>
                <TextBlock 
                IsHitTestVisible="False"
                Text="{Binding Title, ElementName=inputnick_uc}"
                Foreground="Gray"
                FontSize="20"
                FontFamily="pack://application:,,,/krushtype;component/Fonts/#Roboto Mono"
                VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=NameBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                            <Setter Property="Visibility" Value="Hidden"/>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <Button VerticalAlignment="Center"
                    HorizontalAlignment="Right"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    Command="{Binding Command, ElementName=inputnick_uc}"
                    CommandParameter="{Binding Text, ElementName=NameBox}">
                    <Image Source="/Assets/images/check_mark.png" Width="25" Height="25"/>
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Opacity" Value="0"/>
                            <Setter Property="IsHitTestVisible" Value="False"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border x:Name="Border" Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsKeyboardFocused, ElementName=NameBox}" Value="True">
                                                <Setter Property="Opacity" Value="1"/>
                                                <Setter Property="IsHitTestVisible" Value="True"/>
                                            </DataTrigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="Transparent" TargetName="Border" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
