<UserControl x:Class="krushtype.UI.Views.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:krushtype.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="920">

    <Grid>
        <Border Width="450" 
                Height="500"
                Background="#2c2e31"
                CornerRadius="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="120"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" 
                            VerticalAlignment="Center">
                    <Image Source="/Assets/images/icon.png" 
                           Height="60" 
                           Width="60" 
                           Margin="0,0,0,10"/>
                    <TextBlock Text="KrushType" 
                               FontSize="28"
                               FontWeight="SemiBold"
                               Foreground="White"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Login to your account" 
                               FontSize="16"
                               Foreground="#b4b5b8"
                               Margin="0,5,0,0"
                               HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Row="1" 
                            Margin="40,20">
                    <TextBlock Text="Username" 
                               FontSize="14"
                               Foreground="#b4b5b8"
                               Margin="0,0,0,8"/>
                    <Border Background="#323437"
                            CornerRadius="5"
                            Margin="0,0,0,15">
                        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                                 FontSize="14"
                                 Foreground="White"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalContentAlignment="Center"
                                 Padding="10,8"
                                 Height="40"/>
                    </Border>

                    <TextBlock Text="Password" 
                               FontSize="14"
                               Foreground="#b4b5b8"
                               Margin="0,0,0,8"/>
                    <Border Background="#323437"
                            CornerRadius="5"
                            Margin="0,0,0,15">
                        <PasswordBox Name="PasswordBox"
                                     FontSize="14"
                                     Foreground="White"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     VerticalContentAlignment="Center"
                                     Padding="10,8"
                                     Height="40"/>
                    </Border>

                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="#e2b714"
                               FontSize="14"
                               TextWrapping="Wrap"
                               Margin="0,0,0,15"/>

                    <Button Command="{Binding LoginCommand}"
                            Content="Login"
                            FontSize="14"
                            Foreground="White"
                            Background="#e2b714"
                            BorderThickness="0"
                            Padding="0,8"
                            Height="40"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#e2b714"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="5">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#cea912"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                          Margin="0,15,0,0">
                        <TextBlock Text="Loading..." 
                                   FontSize="14"
                                   Foreground="#b4b5b8"
                                   HorizontalAlignment="Center"/>
                    </Grid>
                </StackPanel>                
                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <TextBlock Text="Don't have an account? " 
                               FontSize="14"
                               Foreground="#b4b5b8"/>                    <Button Content="Register"
                            Command="{Binding GoToRegisterCommand}"
                            FontSize="14"
                            Foreground="#e2b714"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="RegisterButton_Click"
                            Margin="5,0,0,0" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
