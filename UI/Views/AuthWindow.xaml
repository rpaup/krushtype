<![CDATA[<Window x:Class="krushtype.UI.Views.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:krushtype.UI.Views"
        xmlns:converters="clr-namespace:krushtype.Core.Utilities.Converters"
        mc:Ignorable="d"
        Title="Authentication" Height="500" Width="450"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/UI/Styles/ThemeColors.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Animation settings -->
            <CubicEase x:Key="EasingFunction" EasingMode="EaseOut"/>
            <Duration x:Key="AnimationDuration">0:0:0.15</Duration>
            <Duration x:Key="StartupAnimationDuration">0:0:0.5</Duration>
            
            <!-- Converters -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
            <converters:BooleanToVisibilityInverseConverter x:Key="BooleanToVisibilityInverseConverter"/>
            <converters:NullOrEmptyToBooleanConverter x:Key="IsNullOrEmptyConverter"/>
            
            <!-- Styles for TextBoxes -->
            <Style x:Key="ModernTextBox" TargetType="{x:Type TextBox}">
                <Setter Property="Height" Value="40"/>
                <Setter Property="Padding" Value="10,0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Background" Value="{DynamicResource SubAltColorBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource SubAltColorBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextColorBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="CaretBrush" Value="{DynamicResource TextColorBrush}"/>
                <Setter Property="SelectionBrush" Value="{DynamicResource MainColorBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TextBox}">
                            <Border x:Name="border" 
                                   Background="{TemplateBinding Background}"
                                   BorderBrush="{TemplateBinding BorderBrush}"
                                   BorderThickness="{TemplateBinding BorderThickness}"
                                   CornerRadius="5"
                                   SnapsToDevicePixels="True">
                                <ScrollViewer x:Name="PART_ContentHost" 
                                             Focusable="False" 
                                             HorizontalScrollBarVisibility="Hidden" 
                                             VerticalScrollBarVisibility="Hidden"
                                             Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation 
                                                    Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                    To="{StaticResource DarkMainColor}" 
                                                    Duration="{StaticResource AnimationDuration}"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation 
                                                    Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                    Duration="{StaticResource AnimationDuration}"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource MainColorBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Style for password boxes -->
            <Style x:Key="ModernPasswordBox" TargetType="{x:Type PasswordBox}">
                <Setter Property="Height" Value="40"/>
                <Setter Property="Padding" Value="10,0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Background" Value="{DynamicResource SubAltColorBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource SubAltColorBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextColorBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="CaretBrush" Value="{DynamicResource TextColorBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type PasswordBox}">
                            <Border x:Name="border" 
                                   Background="{TemplateBinding Background}"
                                   BorderBrush="{TemplateBinding BorderBrush}"
                                   BorderThickness="{TemplateBinding BorderThickness}"
                                   CornerRadius="5"
                                   SnapsToDevicePixels="True">
                                <ScrollViewer x:Name="PART_ContentHost" 
                                             Focusable="False" 
                                             HorizontalScrollBarVisibility="Hidden" 
                                             VerticalScrollBarVisibility="Hidden"
                                             Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation 
                                                    Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                    To="{StaticResource DarkMainColor}" 
                                                    Duration="{StaticResource AnimationDuration}"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation 
                                                    Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                    Duration="{StaticResource AnimationDuration}"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource MainColorBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Style for buttons -->
            <Style x:Key="ModernButton" TargetType="{x:Type Button}">
                <Setter Property="Height" Value="44"/>
                <Setter Property="Background" Value="{DynamicResource MainColorBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource BgColorBrush}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border x:Name="border" 
                                   Background="{TemplateBinding Background}"
                                   BorderBrush="{TemplateBinding BorderBrush}"
                                   BorderThickness="{TemplateBinding BorderThickness}"
                                   CornerRadius="5"
                                   SnapsToDevicePixels="True">
                                <ContentPresenter x:Name="contentPresenter" 
                                                 Focusable="False" 
                                                 HorizontalAlignment="Center" 
                                                 Margin="{TemplateBinding Padding}" 
                                                 RecognizesAccessKey="True" 
                                                 SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                                 VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" TargetName="border" Value="0.8"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Opacity" TargetName="border" Value="0.6"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" TargetName="border" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Style for ghost buttons (text buttons) -->
            <Style x:Key="GhostButton" TargetType="{x:Type Button}">
                <Setter Property="Height" Value="36"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource MainColorBrush}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border x:Name="border" 
                                   Background="{TemplateBinding Background}"
                                   BorderBrush="{TemplateBinding BorderBrush}"
                                   BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter x:Name="contentPresenter" 
                                                 Focusable="False" 
                                                 HorizontalAlignment="Center" 
                                                 Margin="{TemplateBinding Padding}" 
                                                 RecognizesAccessKey="True" 
                                                 SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                                 VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{DynamicResource TextColorBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Opacity" TargetName="contentPresenter" Value="0.6"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" TargetName="contentPresenter" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="Cursor" Value="Hand"/>
            </Style>

            <!-- Style for close/minimize buttons -->
            <Style x:Key="ControlButtonStyle" TargetType="{x:Type Button}">
                <Setter Property="Width" Value="32"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource TextColorBrush}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border x:Name="border" 
                                   Background="{TemplateBinding Background}" 
                                   BorderBrush="{TemplateBinding BorderBrush}" 
                                   BorderThickness="{TemplateBinding BorderThickness}" 
                                   CornerRadius="16">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource SubAltColorBrush}" TargetName="border"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Opacity" Value="0.8" TargetName="border"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="Cursor" Value="Hand"/>
            </Style>

            <!-- Style for password requirement indicators -->
            <Style x:Key="PasswordRequirementStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Margin" Value="0,0,0,4"/>
                <Setter Property="Focusable" Value="False"/>
                <Setter Property="IsHitTestVisible" Value="False"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="Foreground" Value="{DynamicResource SubColorBrush}"/>
            </Style>

            <!-- Style for form labels -->
            <Style x:Key="LabelStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="{DynamicResource TextColorBrush}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="10" Background="{DynamicResource BgColorBrush}">
        <Grid x:Name="mainContainer">
            <Border x:Name="DragBar" Background="Transparent" Height="40" VerticalAlignment="Top" MouseDown="DragBar_MouseDown"/>

            <!-- Login View (Default) -->
            <Grid x:Name="loginView" Margin="32,40,32,30" Visibility="Visible">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="loginHeaderTitle" Grid.Row="0" Text="Sign In" 
                        FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextColorBrush}"
                        HorizontalAlignment="Center" Margin="0,0,0,24">
                    <TextBlock.RenderTransform>
                        <TranslateTransform Y="-15" />
                    </TextBlock.RenderTransform>
                </TextBlock>

                <!-- Username -->
                <StackPanel x:Name="loginUsernamePanel" Grid.Row="1" Margin="0,0,0,16">
                    <StackPanel.RenderTransform>
                        <TranslateTransform Y="-15"/>
                    </StackPanel.RenderTransform>
                    <TextBlock Text="Username" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="txtLoginUsername" Style="{StaticResource ModernTextBox}" 
                             TextChanged="txtLoginUsername_TextChanged"/>
                </StackPanel>

                <!-- Password -->
                <StackPanel x:Name="loginPasswordPanel" Grid.Row="2" Margin="0,0,0,16">
                    <StackPanel.RenderTransform>
                        <TranslateTransform Y="-15"/>
                    </StackPanel.RenderTransform>
                    <TextBlock Text="Password" Style="{StaticResource LabelStyle}"/>
                    <Grid>
                        <PasswordBox x:Name="txtLoginPassword" Style="{StaticResource ModernPasswordBox}" 
                                    PasswordChanged="txtLoginPassword_PasswordChanged"
                                    Visibility="{Binding ElementName=txtLoginPasswordVisible, Path=IsVisible, Converter={StaticResource BooleanToVisibilityInverseConverter}}"/>
                        <TextBox x:Name="txtLoginPasswordVisible" Style="{StaticResource ModernTextBox}" 
                                TextChanged="txtLoginPasswordVisible_TextChanged"
                                Visibility="Collapsed"/>
                        <Button x:Name="btnLoginTogglePassword" Content="👁️" Style="{StaticResource GhostButton}" 
                                HorizontalAlignment="Right" Width="30" Click="btnTogglePassword_Click"
                                Foreground="{DynamicResource SubColorBrush}"/>
                    </Grid>
                </StackPanel>

                <!-- Status message -->
                <Border Grid.Row="3" x:Name="loginStatusContainer" 
                      Background="{DynamicResource SubAltColorBrush}" 
                      BorderThickness="1"
                      BorderBrush="{DynamicResource ErrorColorBrush}"
                      CornerRadius="4"
                      Padding="12,10"
                      Margin="0,0,0,16"
                      Opacity="0"
                      Visibility="Collapsed">
                    <TextBlock x:Name="txtLoginStatus"
                             Foreground="{DynamicResource ErrorColorBrush}"
                             TextWrapping="Wrap"
                             FontSize="12"/>
                </Border>

                <!-- Login button -->
                <StackPanel x:Name="loginButtonsPanel" Grid.Row="4" Margin="0,8,0,0">
                    <StackPanel.RenderTransform>
                        <TranslateTransform Y="-15"/>
                    </StackPanel.RenderTransform>
                    <Button x:Name="btnLogin" Content="Sign In" Style="{StaticResource ModernButton}" Click="btnLogin_Click"/>

                    <!-- Go to Register -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                        <TextBlock Text="Don't have an account? " Foreground="{DynamicResource SubColorBrush}"/>
                        <Button x:Name="btnGoToRegister" Content="Register" Style="{StaticResource GhostButton}" 
                                Height="20" Padding="0" Margin="4,0,0,0" Click="btnGoToRegister_Click"/>
                    </StackPanel>
                </StackPanel>

                <Grid Grid.Row="5"></Grid>
            </Grid>

            <!-- Register View -->
            <Grid x:Name="registerView" Margin="32,40,32,30" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="registerHeaderTitle" Grid.Row="0" Text="Create Account" 
                        FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextColorBrush}"
                        HorizontalAlignment="Center" Margin="0,0,0,24">
                    <TextBlock.RenderTransform>
                        <TranslateTransform Y="-15" />
                    </TextBlock.RenderTransform>
                </TextBlock>

                <!-- Username -->
                <StackPanel x:Name="registerUsernamePanel" Grid.Row="1" Margin="0,0,0,16">
                    <StackPanel.RenderTransform>
                        <TranslateTransform Y="-15"/>
                    </StackPanel.RenderTransform>
                    <TextBlock Text="Username" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="txtRegisterUsername" Style="{StaticResource ModernTextBox}" 
                             TextChanged="txtRegisterUsername_TextChanged"/>
                </StackPanel>

                <!-- Password -->
                <StackPanel x:Name="registerPasswordPanel" Grid.Row="2" Margin="0,0,0,16">
                    <StackPanel.RenderTransform>
                        <TranslateTransform Y="-15"/>
                    </StackPanel.RenderTransform>
                    <TextBlock Text="Password" Style="{StaticResource LabelStyle}"/>
                    <Grid>
                        <PasswordBox x:Name="txtRegisterPassword" Style="{StaticResource ModernPasswordBox}" 
                                    PasswordChanged="txtRegisterPassword_PasswordChanged"
                                    Visibility="{Binding ElementName=txtRegisterPasswordVisible, Path=IsVisible, Converter={StaticResource BooleanToVisibilityInverseConverter}}"/>
                        <TextBox x:Name="txtRegisterPasswordVisible" Style="{StaticResource ModernTextBox}" 
                                TextChanged="txtRegisterPasswordVisible_TextChanged"
                                Visibility="Collapsed"/>
                        <Button x:Name="btnRegisterTogglePassword" Content="👁️" Style="{StaticResource GhostButton}" 
                                HorizontalAlignment="Right" Width="30" Click="btnRegisterTogglePassword_Click"
                                Foreground="{DynamicResource SubColorBrush}"/>
                    </Grid>
                </StackPanel>

                <!-- Password requirements -->
                <StackPanel x:Name="requirementsPanel" Grid.Row="3" Margin="0,0,0,16">
                    <StackPanel.RenderTransform>
                        <TranslateTransform Y="-15"/>
                    </StackPanel.RenderTransform>
                    <TextBlock Text="Password requirements:" Foreground="{DynamicResource SubColorBrush}" 
                            FontSize="11" Margin="0,0,0,4"/>
                    <StackPanel x:Name="passwordRequirements">
                        <TextBlock x:Name="reqLength" Style="{StaticResource PasswordRequirementStyle}">
                            <Run Text="❌" x:Name="iconLength"/>
                            <Run Text="At least 8 characters"/>
                        </TextBlock>
                        <TextBlock x:Name="reqDigit" Style="{StaticResource PasswordRequirementStyle}">
                            <Run Text="❌" x:Name="iconDigit"/>
                            <Run Text="At least 1 number"/>
                        </TextBlock>
                        <TextBlock x:Name="reqUpper" Style="{StaticResource PasswordRequirementStyle}">
                            <Run Text="❌" x:Name="iconUpper"/>
                            <Run Text="At least 1 uppercase letter"/>
                        </TextBlock>
                        <TextBlock x:Name="reqSpecial" Style="{StaticResource PasswordRequirementStyle}">
                            <Run Text="❌" x:Name="iconSpecial"/>
                            <Run Text="At least 1 special character"/>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>

                <!-- Status message -->
                <Border Grid.Row="4" x:Name="registerStatusContainer" 
                      Background="{DynamicResource SubAltColorBrush}" 
                      BorderThickness="1"
                      BorderBrush="{DynamicResource ErrorColorBrush}"
                      CornerRadius="4"
                      Padding="12,10"
                      Margin="0,0,0,16"
                      Opacity="0"
                      Visibility="Collapsed">
                    <TextBlock x:Name="txtRegisterStatus"
                             Foreground="{DynamicResource ErrorColorBrush}"
                             TextWrapping="Wrap"
                             FontSize="12"/>
                </Border>

                <!-- Register button -->
                <StackPanel x:Name="registerButtonsPanel" Grid.Row="5" Margin="0,8,0,0">
                    <StackPanel.RenderTransform>
                        <TranslateTransform Y="-15"/>
                    </StackPanel.RenderTransform>
                    <Button x:Name="btnRegister" Content="Create Account" Style="{StaticResource ModernButton}" Click="btnRegister_Click"/>

                    <!-- Go to Login -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                        <TextBlock Text="Already have an account? " Foreground="{DynamicResource SubColorBrush}"/>
                        <Button x:Name="btnGoToLogin" Content="Sign In" Style="{StaticResource GhostButton}" 
                                Height="20" Padding="0" Margin="4,0,0,0" Click="btnGoToLogin_Click"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <!-- Window Controls -->
            <StackPanel x:Name="windowControls" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,8,8,0">
                <StackPanel.RenderTransform>
                    <TranslateTransform Y="-10"/>
                </StackPanel.RenderTransform>
                <Button x:Name="btnMinimize" 
                        Content="—"
                        Style="{StaticResource ControlButtonStyle}"
                        Margin="0,0,8,0"
                        Click="btnMinimize_Click"/>
                
                <Button x:Name="btnClose" 
                        Content="✕"
                        Style="{StaticResource ControlButtonStyle}"
                        Click="btnClose_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
]]>
